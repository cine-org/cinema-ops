# namespaces

2 namespace: `cinema` (app) và `infra` (Postgres, Redis, External Secrets).

Secret không dùng chéo namespace: app ở `cinema` cần `ExternalSecret` riêng dù secret GSM là một.

- [docs/setup.md](docs/setup.md)
