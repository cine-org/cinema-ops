apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-admin
  namespace: cinema
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - admin.cine.io.vn
      secretName: admin-cine-io-vn-tls
  rules:
    - host: admin.cine.io.vn
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web-admin
                port:
                  name: http
