apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - external-secrets.yaml
  - cluster.yaml

# CR của operator: chờ chart (CRD, webhook) ở wave 0 sẵn sàng rồi mới apply.
commonAnnotations:
  argocd.argoproj.io/sync-wave: "1"
