# web-user

Web cho người dùng cuối, domain `https://cine.io.vn`. Image `ghcr.io/cine-org/setup-monorepo/web-user`, port 80, health `GET /healthz`.

Tag và env ở `envs/staging/`. Chứng chỉ `cine-io-vn-tls` do cert-manager tự xin.

Cách deploy và đổi version: [../docs/setup.md](../docs/setup.md).
